# Offline corpus-maintenance tools only.
# Install from the skill root with:
#   python -m pip install -r scripts/requirements-maintenance.txt

# PDF text extraction for ingest_papers.py
pdfplumber>=0.11.10

# Official exhibition downloader / image-to-PDF reconstruction
requests>=2.34.2
beautifulsoup4>=4.15.0
Pillow>=12.3.0
playwright>=1.45
